# This folder is populated by `pnpm --filter @e4k/mobile cap:add:ios`.
# Capacitor writes the Xcode project into ios/App/ on first run; we keep this
# .gitkeep so the directory exists in git even before that step runs.
