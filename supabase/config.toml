# Supabase local config for English4Kids.
# Generated for Sprint 1 — keep in sync with `supabase/migrations/`.

project_id = "english4kids"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323
api_url = "http://127.0.0.1:54321"

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000"]
jwt_expiry = 3600
enable_signup = true
# Anonymous-first profile model: children play without a parent account; an
# upgrade to a real parent account is a Phase 2 flow.
enable_anonymous_sign_ins = true

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = true

[storage]
enabled = true
file_size_limit = "5MB"

[inbucket]
enabled = true
port = 54324

[analytics]
enabled = false

[functions]
verify_jwt = true
